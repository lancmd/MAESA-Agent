interface:
  display_name: "MAESA Skill"
  short_description: "Apply reproducible ecological analysis to mining areas"
  default_prompt: "Use $mining-area-ecological-analysis to inspect my local project, run or resume the enabled workflow, validate outputs, and package final artifacts safely."
dependencies:
  tools:
    - type: "mcp"
      value: "mining-gis"
      description: "Local ArcGIS, ENVI, PyTorch, PLUS, and InVEST execution service"
      transport: "streamable_http"
      url: "http://127.0.0.1:8765/mcp"
policy:
  allow_implicit_invocation: false
